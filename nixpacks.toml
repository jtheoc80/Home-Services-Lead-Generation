# Nixpacks configuration for Home Services Lead Generation Backend
[variables]
PYTHONPATH = "/app/backend"

[phases.setup]
nixPkgs = ["python310", "postgresql"]

[phases.install]
cmd = "cd backend && pip install -r requirements.txt"

[start]
cmd = "cd backend && python main.py"